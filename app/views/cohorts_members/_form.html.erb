<%= form_for(@cohorts_member) do |f| %>
  <% if @cohorts_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cohorts_member.errors.count, "error") %> prohibited this cohorts_member from being saved:</h2>

      <ul>
      <% @cohorts_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cohort_id %><br>
    <%= f.text_field :cohort_id %>
  </div>
  <div class="field">
    <%= f.label :patient_id %><br>
    <%= f.text_field :patient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
