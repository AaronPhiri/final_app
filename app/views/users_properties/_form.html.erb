<%= form_for(@users_property) do |f| %>
  <% if @users_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_property.errors.count, "error") %> prohibited this users_property from being saved:</h2>

      <ul>
      <% @users_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :property %><br>
    <%= f.text_field :property %>
  </div>
  <div class="field">
    <%= f.label :property_value %><br>
    <%= f.text_field :property_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
